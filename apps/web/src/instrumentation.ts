// Next.js auto-loads this file at startup. Kept as a no-op now that Sentry
// has been removed from the frontend (see next.config.ts for context).
// If you later add another instrumentation provider (OpenTelemetry, custom
// tracing, …), wire it up inside `register()`.
export async function register() {
  // intentionally empty
}
