/**
 * Applo's social media presence. Fill in each URL once the account exists —
 * empty strings are hidden in the footer and omitted from metadata.
 * See docs/guides/SOCIAL_MEDIA_SETUP.md for the account-creation runbook.
 */
export const SOCIAL_LINKS = {
  x: '',
  linkedin: '',
  instagram: '',
  tiktok: '',
};

/** X/Twitter @handle (without the @) for the `twitter:site` meta tag. */
export const X_HANDLE = '';
